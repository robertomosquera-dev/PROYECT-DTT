services:

  redis:

    container_name: dtt-redis

    image: redis:7-alpine

    ports:
      - "${REDIS_PORT}:6379"

    volumes:
      - redis-data:/data

    networks:
      - dtt-network

    restart: unless-stopped
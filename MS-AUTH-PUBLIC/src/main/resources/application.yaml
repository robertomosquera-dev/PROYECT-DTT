spring:
  application:
    name: MS-AUTH-PUBLIC
  config:
    import: configserver:http://${MS_CONFIG:localhost}:${CONFIG_PORT:8888}/

  cloud:
    config:
      fail-fast: true
      retry:
        max-attempts: 10
        initial-interval: 2000
        multiplier: 1.5